# Domain docs

**The estate-wide rule — read the repo's `CONTEXT.md` and `docs/adr/` before
exploring, and proceed silently if either is absent — is defined once in the
`homelab-memory-model` skill** ("Per-repo agent config"). Estate vocabulary is
defined in its owning skill; the collision index is `Homelab-Skills/CONTEXT.md`.
Why: [ADR 0009](https://github.com/cshuttle/Homelab-Skills/blob/main/docs/adr/0009-per-repo-agent-config.md).

Local to this repo:

- No `CONTEXT.md` yet — proceed silently.
