# Triage labels

**The five canonical roles and their label strings are defined once in the
`homelab-memory-model` skill** ("Per-repo agent config"). This file records only
what is local. Why: [ADR 0009](https://github.com/cshuttle/Homelab-Skills/blob/main/docs/adr/0009-per-repo-agent-config.md).

Local to this repo:

- **Overrides**: none. Every label string equals its role name.
